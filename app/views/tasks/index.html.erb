<h1>All the tasks</h1>

<ul>
  <% @tasks.each do |task| %>
<li>
  <%= link_to task.title, task_path(task.id) %>
 <!--  if task.completed == true %>
  check_box("task", task.completed, {checked: true}) %>
  task.title %>
  else  %>
  check_box("task", task.completed, {checked: false}) %>
  task.title %>
  end %> -->
    </li>
<% end %>
</ul>
<%= link_to 'Add a new task', tasks_new_path %>
<%= link_to 'Edit a task', task_edit_path(task.id) %>
